pipeline {
    agent any

    stages {
        
        stage('clone the playbookrepo'){
            steps{
                git branch: 'main', url: 'https://github.com/sheralkaren/Grafanafile.git'
            }
        }
    }
}

